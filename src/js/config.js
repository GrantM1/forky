// Important!!! Never store sensitive data like this, on the JS client side, store sensitive data on server (your server) and use AJAX call to fron-end client
export const proxy = 'https://cors-anywhere.herokuapp.com/';
export const key = 'cf5fd469d3b74a3b9f8da3377cac013f';
// Important!!! Never store sensitive data like this, on the JS client side, store sensitive data on server (your server) and use AJAX call to fron-end client